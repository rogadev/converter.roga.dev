# Converter

In-browser file type converter focusing on image re-encoding (PNG, JPEG, WEBP, AVIF) and MP4 → GIF. No uploads; everything runs locally via Canvas and FFmpeg WASM.

## Development

```
npm i
npm run dev
```

## Tests

```
npm test
```
